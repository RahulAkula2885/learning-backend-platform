<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <!-- Application Name -->
    <property name="APP_NAME" value="learning-app"/>
    <property name="LOG_HOME" value="logs/logging"/>

    <!-- Console Appender -->
    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{correlationId}] [%X{userId}] %-5level %logger{36} - %msg%n
            </pattern>
        </encoder>

    </appender>

    <!-- Main Application Log -->
    <appender name="APP_FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${LOG_HOME}/${APP_NAME}.log</file>

        <rollingPolicy
                class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <!-- Daily log file -->
            <fileNamePattern>
                ${LOG_HOME}/archive/${APP_NAME}.%d{yyyy-MM-dd}.%i.log.gz
            </fileNamePattern>

            <!-- Max file size before creating another file on same day -->
            <maxFileSize>50MB</maxFileSize>

            <!-- Keep logs for 10 days -->
            <maxHistory>10</maxHistory>

            <!-- Total size cap -->
            <totalSizeCap>5GB</totalSizeCap>

        </rollingPolicy>

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{correlationId}] [%X{userId}] %-5level %logger{36} - %msg%n
            </pattern>
        </encoder>

    </appender>

    <!-- Error Log File -->
    <appender name="ERROR_FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${LOG_HOME}/error/error.log</file>

        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>

        <rollingPolicy
                class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <fileNamePattern>
                ${LOG_HOME}/error/error.%d{yyyy-MM-dd}.%i.log.gz
            </fileNamePattern>

            <maxFileSize>20MB</maxFileSize>
            <maxHistory>2</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>

        </rollingPolicy>

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{correlationId}] [%X{userId}] %-5level %logger{36} - %msg%n
            </pattern>
        </encoder>

    </appender>

    <!-- Async Wrapper -->
    <appender name="ASYNC_APP"
              class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>10000</queueSize>
        <appender-ref ref="APP_FILE"/>
    </appender>

    <appender name="ASYNC_ERROR"
              class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>5000</queueSize>
        <appender-ref ref="ERROR_FILE"/>
    </appender>

    <!-- Spring Security Debug Logs -->
    <logger name="org.springframework.security" level="INFO"/>

    <!-- Hibernate SQL Logs (Enable only when needed) -->
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.hibernate.type.descriptor.sql" level="WARN"/>

    <!-- Application Package -->
    <logger name="in.rahul.learning" level="INFO"/>

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="APP_FILE"/>
        <!--        <appender-ref ref="ASYNC_APP"/>-->
        <!--        <appender-ref ref="ASYNC_ERROR"/>-->
    </root>

</configuration>